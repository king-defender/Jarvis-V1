# API: Commands Payload Specification

This document details command JSON structures.

---

## 1. Registry Format
Maps Namespaced Actions (e.g. `career.optimize-resume`) to their required input fields for validation.
