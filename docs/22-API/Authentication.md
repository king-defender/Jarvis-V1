# API: Authentication Handshake Specification

This document details API connection keys.

---

## 1. Credentials
Uses API authorization header keys (`Authorization: Bearer <key>`) to grant command dispatch rights.
