# Controllers Specification

The Controllers Specification outlines express request routing.

---

## 1. Description
Maps API routes to controller classes, parses HTTP bodies, handles errors, and returns JSON.
