# Backend API Specification

The Backend API Specification defines REST endpoints.

---

## 1. Endpoints
* `POST /api/command` - Dispatch execution directive.
* `GET /api/workflow/:id` - Query workflow progress.
* `GET /api/rules` - List rule configurations.
