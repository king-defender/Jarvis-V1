# Repositories Specification

The Repositories Specification maps raw SQLite queries.

---

## 1. Description
Implements repository patterns to interface with tables (`workflows`, `tasks`, `command_directives`).
