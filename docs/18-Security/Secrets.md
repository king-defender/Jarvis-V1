# Secrets Management Specification

This document details credentials storage.

---

## 1. Description
Loads api keys and database credentials from `.env` files locally and avoids checking secrets into Git control.
